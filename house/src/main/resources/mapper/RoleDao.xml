<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dzj.house.dao.RoleDao">

	<select id="getRoleByUserId" resultType="com.dzj.house.entity.Role" parameterType="java.lang.Long">
		select * from role where user_id = #{userId}
	</select>
</mapper>
